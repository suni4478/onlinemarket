apiVersion: v1
kind: Pod
metadata:
  name: onlinemarket
  labels:
    app: onlinemarket
spec:
  containers:
  - name: onlinemarket
    image: sunil4478/project1:v1
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 8080
